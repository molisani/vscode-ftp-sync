
import { RemoteSyncConfig } from "../sync-config";
import { RemoteClient } from "../remote-client";

export function downloadCurrent(config: RemoteSyncConfig, client: RemoteClient): Promise<void> {
    return Promise.resolve();
}
