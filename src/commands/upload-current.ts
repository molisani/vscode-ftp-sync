
import { RemoteSyncConfig } from "../sync-config";
import { RemoteClient } from "../remote-client";

export function uploadCurrent(config: RemoteSyncConfig, client: RemoteClient): Promise<void> {
    return Promise.resolve();
}
